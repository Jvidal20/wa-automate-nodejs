services:
  - type: web
    name: wa-bridge
    env: node
    buildCommand: chmod +x render-build.sh && ./render-build.sh && npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
    plan: free
