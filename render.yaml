services:
  - type: web
    name: wa-bridge
    env: node
    buildCommand: bash render-build.sh && npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
    plan: free
